apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: default
spec:
  selector:
    app: sample-app
  ports:
    - protocol: TCP
      name: http # Named port is best practice for ServiceMonitor
      port: 80
      targetPort: 8080
  type: ClusterIP # We will use Ingress or LoadBalancer later
